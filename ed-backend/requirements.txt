# Web framework
Django>=5.0,<5.1
djangorestframework>=3.15,<3.16
django-cors-headers

# Database
psycopg[binary]>=3.1
dj-database-url>=2.1

# Serving / static
gunicorn>=22.0
whitenoise>=6.6

# Media (ImageField in quests/stickers)
Pillow>=10.3

# ML (GMM clustering). The .pkl models were saved with scikit-learn 1.8.0,
# so match that to avoid InconsistentVersionWarning / invalid results.
# numpy pinned <2 for pickle-array compatibility.
numpy<2
scipy>=1.11
scikit-learn>=1.8,<1.9
joblib>=1.3
pandas>=2.0

# Config
python-dotenv>=1.0
